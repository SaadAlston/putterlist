import React from "react";
import ReactDOM from "react-dom/client";
import PutterList from "./PutterList.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PutterList />
  </React.StrictMode>
);
